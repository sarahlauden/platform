{
  "environments": {
    "test": {
      "buildpacks": [
        { "url": "https://github.com/heroku/heroku-buildpack-google-chrome" },
        { "url": "heroku/ruby" }
      ],
      "addons": ["heroku-postgresql:in-dyno"],
      "scripts": {
        "test-setup": "bundle exec rake webpacker:install",
        "test": "bundle exec rspec"
      }
    }
  }
}
